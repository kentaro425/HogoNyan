<%= render 'public/posts/header' %>
<div class="container">
  <div class="row">
    <div class="col-6 mx-auto">
      <div class="row mb-3">
        <ul class="nav nav-tabs justify-content-center" role="tablist">
          <li class="nav-item">
            <a class="nav-link active" id="item1-tab" data-toggle="tab" href="#item1" role="tab" aria-controls="item1" aria-selected="true">投稿検索</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" id="item2-tab" data-toggle="tab" href="#item2" role="tab" aria-controls="item2" aria-selected="false">ユーザー検索</a>
          </li>
        </ul>
        <div class="tab-content">
          <div class="tab-pane fade show active" id="item1" role="tabpanel" aria-labelledby="item1-tab">
            <%= search_form_for @p, url: search_posts_path, class: "input-group" do |f| %>
              <%= f.text_field :tags_name_or_user_nickname_eq, placeholder: "キーワードから探す", class: "form-control" %>
              <%= f.submit "検索", class: "btn btn-success" %>
            <% end %>
          </div>
          <div class="tab-pane fade" id="item2" role="tabpanel" aria-labelledby="item2-tab">
            うy
          </div>
        </div>
      </div>
      <% @posts.each do |post| %>
        <div class="row">
          <div class="card mx-auto" style="width: 30rem;">
            <%= link_to sns_home_user_path(post.user.id) do %>
              <%= post.user.nickname %>
            <% end %>
            <div>
              <% if post.post_images.count > 0 %>
                <%= image_tag post.post_images.first, class: "img-thumbnail" %>
              <% end %>
            </div>
            <div class="card-body">
              <div id="favorite_buttons_<%= post.id %>">
          　　  　<%= render "public/sns_favorites/favorites", post: post %>
        　     </div>
              <p class="card-text"><%= post.body %></p>
              <p class="card-text">🐱<%= post.tags.map(&:name).join(' 🐱') %></p>
              <%= link_to post_path(post) do %>
                <p class="card-text text-secondary">
                コメント<%= post.comments.count %>件をすべて見る
                </p>
              <% end %>
              <%= render 'public/comments/form', comment: @comment, post: post %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>